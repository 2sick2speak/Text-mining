# header for libraries

require(tm)
require(RTextTools)
require(dplyr)
require(randomForest)
require(RWeka)
require(tau)
require(e1071) 
require(openNLP)

setwd("~/Dropbox/Public/DMTrack/Kaggle-ASAP-AES")